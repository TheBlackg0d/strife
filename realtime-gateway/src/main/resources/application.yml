spring:
  application:
    name: realtime-gateway

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health,info

# TODO au fur et à mesure que tu construis ce service :
#   - spring.datasource.* (si ce service a sa propre base de données — voir doc/architecture/services-overview.md)
#   - spring.rabbitmq.* (host/port/credentials — voir docker-compose.yml à la racine)
#   - toute autre config spécifique à ce service
