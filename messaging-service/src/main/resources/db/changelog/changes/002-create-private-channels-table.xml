<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="002-create-private-channels-table" author="Moncles Junior Herisse">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="private_channels"/>
            </not>
        </preConditions>

        <createTable tableName="private_channels">
            <column name="id" type="uuid">
                <constraints primaryKey="true" primaryKeyName="pk_private_channels" nullable="false"/>
            </column>
            <column name="channel_name" type="VARCHAR(255)"/>
            <!-- boolean primitif côté entité : jamais null, d'où le NOT NULL. -->
            <column name="show_channel" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>
